# Database Settings
#---------------------------------
# N8N Version (defaults to 'latest' if not set)
N8N_VERSION=latest

# Database type (sqlite, postgresdb, mysqldb, mongodb)
DB_TYPE=sqlite

# General database settings (required for PostgreSQL/MySQL)
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=n8n
DB_USER=root
DB_PASSWORD=secure_password

# Special connection URL (required for MongoDB)
# Example: mongodb://user:password@localhost:27017/n8n
DB_CONNECTION_URL=

# SQLite path (only if using SQLite)
DB_SQLITE_PATH=/home/node/.n8n/database.sqlite

# Task Runners
#---------------------------------
# Enable task runners for secure code execution
N8N_RUNNERS_ENABLED=true

# Runner execution mode (internal or external)
N8N_RUNNERS_MODE=internal

# Maximum task execution time in seconds
N8N_RUNNERS_TIMEOUT=60

# Maximum number of concurrent tasks
N8N_RUNNERS_MAX_CONCURRENCY=5

# Task runners broker port
N8N_RUNNERS_BROKER_PORT=5679

# Broker listen address
N8N_RUNNERS_BROKER_LISTEN_ADDRESS=0.0.0.0

# Basic Authentication
#---------------------------------
# Enable basic authentication
N8N_BASIC_AUTH_ACTIVE=false

# Authentication credentials
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=very_secure_password

# General Settings
#---------------------------------
# Base URL for webhooks
WEBHOOK_URL=http://localhost:5678

# Main n8n port
N8N_PORT=5678

# Timezone configuration
GENERIC_TIMEZONE=UTC

# Encryption key (CHANGE THIS in production!)
# Generate using: openssl rand -hex 32
N8N_ENCRYPTION_KEY=your_super_secret_key_here 